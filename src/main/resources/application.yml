server:
  port: 9001

jwt:
  secret: ${JWT_SECRET:sigur-chat-very-secure-jwt-secret-key-change-in-production-2026}
  expiration: 86400000

logging:
  level:
    root: INFO
    com.example.webrtcchat: DEBUG